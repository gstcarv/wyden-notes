{
    "name": "wyden-notes",
    "version": "1.0.0",
    "scripts": {
        "build": "cd client && npm install && npm install --dev && npm run build && cd ..",
        "start": "cd server && npm install && node server.js"
    },
    "heroku-run-build-script": true,
    "engines": {
        "node": "16.13.2"
    }
}
