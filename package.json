{
    "name": "wyden-notes",
    "version": "1.0.0",
    "scripts": {
        "build": "cd client && npm install && npm run build && cd ..",
        "start": "cd server && npm install && npm start"
    }
}
